---
import { navLinks, site, socials } from '../data/site';
import Icon from './Icon.astro';
import Button from './Button.astro';
---

<footer class="footer">
  <div class="container footer__grid">
    <div>
      <p class="eyebrow">Data engineering practice</p>
      <h2 class="headline" style="font-size: var(--step-3);">{site.name}</h2>
      <p class="subhead">{site.availability}</p>
    </div>
    <div>
      <p class="kicker">Quick links</p>
      <div class="footer__links">
        {
          navLinks.map((link) => (
            <a class="nav__link" href={link.href}>
              {link.label}
            </a>
          ))
        }
      </div>
    </div>
    <div>
      <p class="kicker">Connect</p>
      <div class="socials">
        {
          socials.map((social) => (
            <a href={social.href} aria-label={social.label} target="_blank" rel="noreferrer">
              <Icon name={social.icon} size={18} />
              {social.label}
            </a>
          ))
        }
      </div>
      <p class="kicker" style="margin-top: var(--space-4);">
        {site.location}
      </p>
    </div>
  </div>
  <div class="container footer__cta">
    <p class="footer__cta-text">
      <strong>Available for new projects.</strong> Let's discuss your data engineering needs.
    </p>
    <Button href="/contact" variant="outline">Get in touch</Button>
  </div>
  <div class="container">
    <p class="footer__copyright">
      &copy; {new Date().getFullYear()} {site.name}. Built with Astro.
    </p>
  </div>
</footer>
